# CopyLists

> `macOS 13+` · `原生 Swift` · `纯本地` · `MIT`

## 你的剪贴板，终于有了记忆

每天你复制粘贴几十次，却只有一条内容能留下。
CopyLists 默默记录每一次复制——文字、链接、代码、截图，一个快捷键随时取用。

---

## 核心功能

### ⚡️ 呼之即来，挥之即去
任意界面按下 `⌘⇧V`，历史面板瞬间浮现于屏幕中央。选好内容按回车，自动切回原应用完成粘贴，全程不超过 2 秒。

### 🖼️ 截图也是历史的一部分
微信截图、系统截图、网页复制的图片，一律自动入库并生成缩略图预览。需要时选中按回车，原图直接还原到剪贴板。

### ♻️ 再也不会有重复条目
同一段内容复制了三次？列表里只有一条，使用次数安静地累加在角落。历史永远整洁，不需要手动清理。

### 🏷️ 六种类型，一眼看清
自动识别 `URL` / `邮箱` / `文件路径` / `代码片段` / `截图` / `纯文本`，顶部标签栏一键过滤，左右方向键也能切换。

### ⌨️ 全程不碰鼠标
`↑↓` 选条目 · `←→` 切标签 · `↵` 粘贴 · `⌘⌫` 删除 · `⎋` 关闭。从呼出到粘贴，双手始终在键盘上。

### 🔒 数据只在你的电脑里
无账号、不联网、不上传。所有内容存储在本机 `~/Library/Application Support/CopyLists/`，卸载即清除，没有任何后顾之忧。

---

## 快捷键速查

| 按键 | 动作 |
|:---:|---|
| `⌘⇧V` | 唤起 / 隐藏面板 |
| `↑` / `↓` | 选择条目 |
| `←` / `→` | 切换类型标签 |
| `↵` | 粘贴到当前应用 |
| `⌘⌫` | 删除选中条目 |
| `⎋` | 关闭面板 |

---

## 数字说话

| | |
|:---:|---|
| **50** | 条历史上限，LRU 算法自动淘汰最久未用的内容 |
| **6** | 种内容类型，自动识别无需手动分类 |
| **0** | 字节离开本机，完全离线运行 |
| **1** | 个快捷键，覆盖所有使用场景 |

---

## 安装方式

### 方式一：下载安装包（推荐）

1. 下载根目录下 `CopyLists.dmg.zip` 解压
2. 双击挂载，将 `CopyLists.app` 拖入 `Applications` 文件夹
3. 右键 → **打开**（首次需绕过 Gatekeeper）

### 方式二：源码构建

```bash
git clone git@github.com:evenjxr/copyLists.git && cd copyLists
bash build_app.sh
open CopyLists.app
```

### 授权说明

首次启动时，请前往 **系统设置 → 隐私与安全性 → 辅助功能**，开启 CopyLists 的权限。
该权限仅用于模拟 `⌘V` 粘贴动作，不读取任何其他系统数据。

---

*CopyLists · macOS 13+ · MIT License · Made with Swift ❤️*
